pasos a seguir:
# Arquitectura Técnica Óptima para SaaS de Béisbol

Basado en análisis exhaustivo de tecnologías actuales, implementaciones exitosas en deportes y mejores prácticas de desarrollo, esta investigación proporciona la arquitectura técnica definitiva para tu aplicación SaaS de béisbol con NextJS, GraphQL y optimización móvil.

## Stack Tecnológico Recomendado

**Supabase emerge como la solución integral preferida** para tu base de datos con soporte GraphQL nativo. Con 500MB de almacenamiento gratuito, 50,000 usuarios activos mensuales y capacidades ilimitadas de API, maneja cómodamente múltiples ligas con 5-18 equipos cada una. Su implementación GraphQL mediante pg_graphql genera automáticamente APIs desde el esquema PostgreSQL, mientras que las funciones Row Level Security (RLS) proporcionan aislamiento perfecto de datos por liga.

La arquitectura NextJS 14+ con App Router ofrece el patrón óptimo para aplicaciones SaaS multi-tenant. El enrutamiento dinámico `[subdomain]` permite separación limpia de ligas, mientras que los Server Components optimizan la carga de datos deportivos. Apollo Client se establece como la mejor opción para el cliente GraphQL, con caché inteligente para estadísticas frecuentemente consultadas y suscripciones WebSocket para actualizaciones en tiempo real durante los juegos.

## Arquitectura de Base de Datos y Modelos

### Estructura de datos optimizada para béisbol

El modelado de datos se basa en patrones probados por la MLB y sistemas deportivos de alto rendimiento. Las entidades principales incluyen Liga → Temporada → Equipo → Jugador → Juego → Estadísticas, con relaciones optimizadas para consultas complejas:

```graphql
type Liga {
  id: ID!
  nombre: String!
  codigo: String! # Para registro de jugadores
  equipos: [Equipo!]!
  temporadas: [Temporada!]!
  configuracion: ConfiguracionLiga!
}

type Juego {
  id: ID!
  fecha: DateTime!
  equipoLocal: Equipo!
  equipoVisitante: Equipo!
  marcadorLocal: Int
  marcadorVisitante: Int
  estado: EstadoJuego!
  entradas: [Entrada!]!
  estadisticas: [EstadisticaJuego!]!
}

type EstadisticasBateador {
  jugador: Jugador!
  temporada: Temporada!
  juegosJugados: Int!
  turnos: Int!
  hits: Int!
  carreras: Int!
  impulsadas: Int!
  promedioBateo: Float! # Campo calculado
}
```

**Patrones de optimización** incluyen estadísticas pre-calculadas para consultas rápidas, índices específicos para búsquedas por equipo y temporada, y estructuras desnormalizadas para agregaciones frecuentes como rankings y promedios de bateo.

### Multi-tenancy con Row Level Security

La implementación RLS de PostgreSQL proporciona aislamiento seguro a nivel de base de datos:

```sql
-- Habilitar RLS en todas las tablas
ALTER TABLE juegos ENABLE ROW LEVEL SECURITY;

-- Política específica por liga
CREATE POLICY liga_aislamiento ON juegos 
  FOR ALL TO app_user 
  USING (liga_id = current_setting('app.liga_id')::uuid);
```

Esta aproximación permite una sola base de datos compartida con seguridad garantizada entre ligas, reduciendo costos operacionales mientras mantiene la separación de datos necesaria para un modelo SaaS.

## Arquitectura NextJS Optimizada para Claude Code

### Estructura de proyecto AI-friendly

La organización del código optimizada para desarrollo con Claude Code utiliza nomenclatura descriptiva y co-localización de recursos:

```
src/
├── components/
│   ├── beisbol/
│   │   ├── TableroPuntuacion.tsx
│   │   ├── TarjetaJugador.tsx
│   │   └── FormularioEstadisticas.tsx
│   └── ui/ # Componentes shadcn/ui
├── lib/
│   ├── apollo/
│   │   ├── client.ts
│   │   └── queries.ts
│   ├── auth/
│   └── utils/
├── types/
│   ├── beisbol.ts
│   ├── liga.ts
│   └── api.ts
└── app/
    ├── [subdominio]/
    │   ├── dashboard/
    │   ├── juegos/
    │   └── estadisticas/
    └── api/
```

**TypeScript integral** mejora significativamente la generación de código por IA, con interfaces detalladas que proporcionan contexto completo para asistentes de código. Los comentarios JSDoc exhaustivos y ejemplos de uso facilitan la comprensión del contexto por parte de Claude.

### Patrón de enrutamiento multi-tenant

El middleware NextJS maneja la resolución de subdominios automáticamente:

```typescript
export function middleware(request: NextRequest) {
  const hostname = request.headers.get('host');
  const subdomain = hostname?.split('.')[0];
  
  if (subdomain && subdomain !== 'www') {
    return NextResponse.rewrite(
      new URL(`/${subdomain}${request.nextUrl.pathname}`, request.url)
    );
  }
}
```

Este patrón permite URLs como `miliga.tuapp.com` con separación limpia de contexto por liga, optimizando tanto SEO como experiencia de usuario.

## Sistema de Notificaciones Integral

### WhatsApp Business API con Novu

**Novu Echo emerge como la solución más elegante** para notificaciones WhatsApp, combinando API gratuita para desarrollo con integración nativa NextJS. La implementación basada en workflows permite mensajes complejos:

```javascript
echo.workflow("notificacion-juego", async ({ step }) => {
  await step.chat("send-whatsapp", async () => {
    return {
      body: "🏟️ Juego programado: {{equipoLocal}} vs {{equipoVisitante}} el {{fecha}} a las {{hora}}"
    };
  });
});
```

Los templates pre-aprobados incluyen horarios de juegos, resultados finales, alertas meteorológicas y anuncios de liga, todos con variables dinámicas y mecanismos de opt-out integrados.

### Email transaccional con SendGrid

SendGrid proporciona el equilibrio óptimo entre características y costo para ligas deportivas. Su nivel gratuito de 100 emails diarios es perfecto para ligas pequeñas, mientras que la escalabilidad permite crecimiento sin cambios arquitectónicos. Los templates dinámicos facilitan mensajes personalizados:

```javascript
const mensaje = {
  to: usuario.email,
  from: 'liga@tuapp.com',
  templateId: 'd-notificacion-juego',
  dynamic_template_data: {
    nombreJugador: usuario.nombre,
    fechaJuego: juego.fecha,
    oponente: juego.oponente,
    ubicacion: juego.estadio
  }
};
```

## Autenticación y Autorización Robusta

### NextAuth.js con contexto multi-tenant

NextAuth.js combinado con Supabase Auth proporciona la solución más costo-efectiva y completa. La implementación maneja tres roles específicos:

- **Admin de Liga**: Gestión completa de liga, equipos y calendario
- **Anotador**: Captura de estadísticas y actualización de marcadores
- **Jugador**: Acceso a perfil personal y estadísticas del equipo

```typescript
export const authOptions: NextAuthOptions = {
  callbacks: {
    jwt: async ({ user, token }) => {
      if (user) {
        token.role = user.role;
        token.ligaId = user.ligaId;
      }
      return token;
    },
    session: async ({ session, token }) => {
      session.user.role = token.role;
      session.user.ligaId = token.ligaId;
      return session;
    },
  },
};
```

**Protección de rutas basada en roles** utiliza middleware NextJS para validación automática, mientras que el sistema de códigos de liga permite registro controlado de nuevos usuarios.

## Optimización Móvil Prioritaria

### Diseño Progressive Web App (PWA)

La implementación PWA es crucial para aplicaciones deportivas, permitiendo funcionalidad offline durante juegos en ubicaciones con conectividad limitada. Next-PWA proporciona service workers automáticos con estrategias de caché inteligentes:

```javascript
const withPWA = require('next-pwa')({
  dest: 'public',
  runtimeCaching: [
    {
      urlPattern: /^https:\/\/api\.tuapp\.com/,
      handler: 'NetworkFirst',
      options: {
        cacheName: 'api-cache',
        expiration: { maxEntries: 100, maxAgeSeconds: 300 }
      }
    }
  ]
});
```

**Capacidades offline** incluyen entrada de marcadores local con sincronización automática al restaurar conectividad, usando almacenamiento local SQLite y colas de sincronización en background.

### UI optimizada para touch con Tailwind

Los patrones específicos para deportes incluyen tarjetas de puntuación deslizables, actualizaciones pull-to-refresh para datos en vivo, formularios de entrada optimizados para touch, y navegación basada en gestos. La implementación de modo oscuro con colores pasteles mantiene la identidad visual mientras reduce fatiga ocular durante sesiones prolongadas.

## Funcionalidades de Exportación Avanzadas

### Generación PDF escalable

Para reportes complejos como brackets de eliminatorias y estadísticas completas, la generación server-side con Puppeteer ofrece rendering consistente:

```javascript
const pdf = await page.pdf({
  format: 'A4',
  printBackground: true,
  margin: { top: '1cm', bottom: '1cm', left: '1cm', right: '1cm' },
  displayHeaderFooter: true,
  headerTemplate: '<div style="font-size: 10px;">{{nombreLiga}} - Temporada {{año}}</div>'
});
```

Los templates incluyen calendarios de temporada, hojas de roster, resúmenes estadísticos y brackets de torneos con layouts responsivos que se adaptan automáticamente al tamaño del torneo.

### Exportación Excel con características deportivas

La implementación maneja grandes conjuntos de datos mediante procesamiento streaming, evitando problemas de memoria. Las características específicas incluyen tablas dinámicas pre-configuradas para análisis de rendimiento, formato condicional para destacar estadísticas clave, y plantillas personalizables para diferentes tipos de reportes.

## Sistema de Eliminatorias Configurable

La flexibilidad del sistema de playoffs soporta múltiples formatos:

```graphql
type Eliminatoria {
  id: ID!
  temporada: Temporada!
  formato: FormatoEliminatoria! # SIMPLE, DOBLE, MEJOR_DE_SERIES
  rondas: [RondaEliminatoria!]!
  equipos: [EquipoEliminatoria!]!
}

enum FormatoEliminatoria {
  ELIMINACION_SIMPLE
  ELIMINACION_DOBLE
  MEJOR_DE_SERIES
  ROUND_ROBIN
}
```

Los algoritmos de bracket adaptan automáticamente el layout según el número de participantes, con generación PDF optimizada para impresión en formatos estándar.

## Consideraciones de Despliegue en Vercel

### Edge Functions para datos deportivos

Las Edge Functions optimizan la entrega de datos en tiempo real, especialmente para actualizaciones de marcadores durante juegos en vivo. La distribución global reduce latencia crítica para experiencias en tiempo real.

**Gestión de variables de entorno** incluye configuraciones específicas por liga, claves API seguras para servicios de notificación, y flags de características para rollouts controlados. La integración CI/CD automatiza testing, despliegue y rollback para mantener alta disponibilidad durante temporadas deportivas críticas.

## Conclusiones y Próximos Pasos

Esta arquitectura proporciona la base técnica sólida para un SaaS de béisbol escalable y centrado en el usuario. La combinación Supabase + NextJS + Apollo Client ofrece el equilibrio óptimo entre funcionalidad, costo y facilidad de desarrollo con Claude Code.

**Implementación recomendada por fases**: Comenzar con funcionalidades core de gestión de liga (Fase 1), agregar scoring en tiempo real y optimización móvil (Fase 2), expandir con analytics avanzados y capacidades de exportación (Fase 3), y finalmente implementar arquitectura multi-tenant completa para escalabilidad SaaS (Fase 4).

La arquitectura documentada garantiza 1 año de acceso a datos, escalabilidad a múltiples ligas, y optimización UX sin comprometer funcionalidad técnica, cumpliendo todos los requerimientos específicos del proyecto.